{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "include": ["**/*.ts", "**/*.tsx", ".expo/types/**/*.ts", "expo-env.d.ts"],
  "exclude": [
    "node_modules",
    "tests",
    "playwright-report",
    "test-results",
    "allure-results",
    "allure-report",
    "reports",
    ".expo",
    "supabase"
  ]
}
