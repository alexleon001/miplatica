# Hoist node_modules to a flat layout (npm-style) instead of pnpm's default
# symlinked .pnpm/* tree. Expo + Metro + Expo Router don't play well with
# the symlinked tree and crash with "path must be string, received Object"
# on first bundle. Hoisted is slower but stable for RN/Expo projects.
node-linker=hoisted
